require("config").init()

return {
  { "folke/lazy.nvim", version = "*" },
  --{ dir = "../config/init", name = "luavim", priority = 10000, lazy = false, config = function (plug, opts) plug.setup(opts)  end, cond = true, version = "*" },
}
